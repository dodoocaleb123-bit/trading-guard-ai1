# Replacement Intelligence v4 Activation Verification

The overview now identifies Replacement Intelligence v4 as the active decision layer, and the Winning Rate page labels the v4 outcome card as active. The official macro panel states that its context is available to active v4. Historical v2/v3 records remain separately visible.

Desktop preview verification showed the Overview and Winning Rate pages render without clipping, with the active v4 wording visible and the existing paper-only/UNVALIDATED guardrails preserved. The current production-facing data showed open paper signals and the loss-learning review panel remained available.

The scanner code now creates/activates a v4 intelligence version when the current active version is not v4, evaluates v4 for delivered signals, retains v3 as a baseline trace, and manual audits use v4. Outcome summaries query the v4 version. Full Vitest and production build passed after the activation changes.
